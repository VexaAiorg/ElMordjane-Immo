# Frontend Environment Variables Example
# Copy this to .env.production for production builds

# API Base URL 
# Leave empty for production (uses relative paths with nginx proxy)
# For local development with separate backend:
# VITE_API_BASE_URL=http://localhost:3000

VITE_API_BASE_URL=
